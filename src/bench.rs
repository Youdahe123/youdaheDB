// Benchmark. Drives the same workload at rustdb and at real Redis through the
// same client, reporting throughput plus p50/p99 latency. Talks to the server
// over TCP like any other client.
// Empty for now.
